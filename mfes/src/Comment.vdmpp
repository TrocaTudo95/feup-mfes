class Comment
types 
	public String = seq1 of char;
	
instance variables
  content:seq of (char);
  user: User;
  
operations
  public Comment: String*User ==> Comment 
  Comment(c,u)	==(	content :=c;
		user := u;
		return self);

end Comment
