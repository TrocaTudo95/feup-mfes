class User
instance variables
  Comments:set of (Comment);
  Posts:set of (Post);
  Friends: set of(User);
  BirthdayDate:seq of (char);
  Email:seq of (char);
  Name:seq of (char);
  Password:seq of (char);
  
  

end User
